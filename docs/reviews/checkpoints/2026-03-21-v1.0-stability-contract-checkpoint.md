# Checkpoint: v1.0 Stability Contract

- 时间：`2026-03-21 08:22:46 CST`
- 分支：`codex/v1.0-stability-contract`
- 远端：`origin/codex/v1.0-stability-contract`
- 当前头提交：`dba161f test(ui): enforce protocol registry coverage`

## 本阶段已完成

- 空状态客户预览不再写死 `C001`，改为使用 bootstrap 下发的真实 `preview_customer_id`
- 运行时默认品牌、导购、门店身份改成中性值，避免未配置环境暴露 demo 身份
- 空状态与桌面侧栏的详情入口改成中性文案，并在没有预览客户时禁用
- bootstrap 快捷问题改成“配置优先，缺省时按当前客户/偏好/待办动态生成”
- 新增协议同步测试，自动校验后端 `component_type` / `action_type` 已在前端 registry 注册

## 最近已推送提交

- `dba161f` `test(ui): enforce protocol registry coverage`
- `ecf060c` `refactor(ui): derive bootstrap quick prompts from live data`
- `3c11acd` `refactor(ui): disable empty-state preview when unavailable`
- `72c441f` `refactor(core): neutralize runtime identity defaults`
- `e45f82a` `refactor(ui): source empty-state preview from bootstrap`

## 当前回归结果

- `./.venv/bin/pytest`：`45 passed`
- `pnpm test -- --run`：`19 passed`
- `pnpm build`：passed
- `pnpm exec playwright test`：`14 passed`

## 当前工作区状态

- 已同步到远端，功能分支无未提交代码改动
- 仍有一份用户自己的未提交文档：
  - `docs/roadmap/crm-agent-master-plan.md`
- 该文件本 checkpoint 未纳入任何提交，也没有被回退

## 下一步建议

- 继续沿 `v1.0.0 去写死与工程底座` 主线清理剩余运行时伪默认行为
- 优先补“后端协议 -> 前端渲染/动作”之外的稳定性门禁，例如更多语义回归和实体稳定/文本可变评测
- 保持“小任务 -> 全量回归 -> 提交 -> 推送”的节奏，不跨大步合并
