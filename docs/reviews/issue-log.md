# CRM Agent 问题记录

## 使用方式

开发和测试过程中发现的问题，都要记录在这里，并在修复后补状态。

## 记录模板

### 问题标题

- 发现版本：
- 场景：
- 现象：
- 复现步骤：
- 根因分类：
  - 计划问题 / 执行问题 / 数据问题 / 协议问题 / 测试缺失
- 修复动作：
- 是否补进回归：
- 当前状态：

## 当前已记录问题

### Demo 时期大量写死策略

- 发现版本：`v0.1.0` 前
- 场景：客户筛选、推荐数量、固定组件组合
- 现象：系统行为更像固定模板，而不是商业化导购产品
- 复现步骤：连续提问筛选和推荐类问题时，可看到固定数量、固定组合、固定表达倾向
- 根因分类：计划问题 + 执行问题
- 修复动作：纳入 `v1.0.0` 去写死与工程底座
- 是否补进回归：是
- 当前状态：待修复

### 重复提问被缓存误判为 fresh

- 发现版本：`v1.0.0` 开发中
- 场景：同会话重复问同一个推荐问题
- 现象：系统直接命中上一轮缓存，导致 `repeat_query_mode` 一直显示为 `fresh`
- 复现步骤：连续两次提问“给乔知夏推荐一些产品，并且说明理由”
- 根因分类：执行问题
- 修复动作：重复提问模式下绕过前置缓存，并新增 `preserve/diversify` 测试
- 是否补进回归：是
- 当前状态：已修复

### 客户总览样本数和列表结果不一致

- 发现版本：`v1.0.0` 开发中
- 场景：配置 `CRM_CUSTOMER_SAMPLE_LIMIT`
- 现象：概览卡的 `sample_limit` 已更新，但客户列表仍按旧默认值截断
- 复现步骤：设置 `CRM_CUSTOMER_SAMPLE_LIMIT=6` 后请求“现在有哪些客户”
- 根因分类：执行问题
- 修复动作：客户总览查询改为显式使用配置样本数，并补配置回归测试
- 是否补进回归：是
- 当前状态：已修复

### 可见结果和会话状态存在隐藏 `4` 条截断

- 发现版本：`v1.0.0` 开发中
- 场景：商品推荐、客户维护、任务结果回写会话状态
- 现象：前端已展示完整结果，但 `focus_scope`、`last_entity_ids` 和部分测试仍按旧的 `4` 条截断
- 复现步骤：提问“找6件适合夏天穿的衣服”或进入客户维护链路，检查响应里的 `product_grid` 与 `session_snapshot`
- 根因分类：执行问题 + 测试缺失
- 修复动作：统一按当前可见商品/任务集合写入响应组件、`focus_scope`、`session_snapshot` 和最终实体集合，并补后端回归测试与前端/E2E 断言修正
- 是否补进回归：是
- 当前状态：已修复

### 工作流说明和记忆摘要预览条数仍写死在服务层

- 发现版本：`v1.0.0` 开发中
- 场景：`workflow_checkpoint` 与 `memory_briefs`
- 现象：即使其他业务数量已配置化，这两个预览仍固定按实现层条数裁剪
- 复现步骤：分别设置 `CRM_WORKFLOW_NOTE_LIMIT`、`CRM_MEMORY_BRIEF_LIMIT` 预期值后查看响应
- 根因分类：执行问题
- 修复动作：新增配置项，并补回归测试验证工作流说明和客户记忆摘要都会按配置返回
- 是否补进回归：是
- 当前状态：已修复

### 运行时代码仍残留商品标签上限和默认数量字面量

- 发现版本：`v1.0.0` 开发中
- 场景：商品卡标签预览与 `GuardrailResult` 默认值
- 现象：即使主链路数量已配置化，仍有运行时代码继续使用字面量 `4`
- 复现步骤：设置 `CRM_PRODUCT_TAG_LIMIT` 或 `CRM_DEFAULT_RESULT_COUNT` 后，检查商品标签与 guardrail 默认对象
- 根因分类：执行问题
- 修复动作：把商品标签预览上限迁入配置层，并将 `GuardrailResult.requested_count` 默认值改为读取配置
- 是否补进回归：是
- 当前状态：已修复

### 前端头部会伪造 demo 品牌和导购信息

- 发现版本：`v1.0.0` 开发中
- 场景：`/crm` 主工作台头部
- 现象：当 bootstrap 字段为空时，界面会回退到 `缦序 / 林顾问 / 上海静安店`
- 复现步骤：让 `brand_name`、`advisor_name`、`store_name` 为空后打开工作台
- 根因分类：执行问题
- 修复动作：移除展示层硬编码兜底，标题和元信息改为中性占位文案，并补前端回归测试
- 是否补进回归：是
- 当前状态：已修复

### 聊天入口限流阈值仍写死在 API 层

- 发现版本：`v1.0.0` 开发中
- 场景：`POST /api/crm/chat/send`
- 现象：聊天限流虽然已接入，但阈值和窗口时长仍固定写在路由里
- 复现步骤：尝试通过环境变量调整聊天限流，接口行为不会变化
- 根因分类：执行问题
- 修复动作：把聊天限流的阈值和窗口时长迁入配置层，并补 API 集成测试验证 `429`
- 是否补进回归：是
- 当前状态：已修复

### mutation 限流阈值仍写死在 API 层

- 发现版本：`v1.0.0` 开发中
- 场景：任务完成、记忆审批、记忆拒绝
- 现象：mutation 已有限流，但阈值和窗口时长仍固定写在路由里
- 复现步骤：设置 mutation 限流环境变量后连续触发接口，行为不会变化
- 根因分类：执行问题
- 修复动作：把 mutation 限流阈值和窗口时长迁入配置层，并补 API 集成测试验证 `429`
- 是否补进回归：是
- 当前状态：已修复

### 限流后前端仍显示通用失败文案

- 发现版本：`v1.0.0` 开发中
- 场景：聊天主输入区
- 现象：即使后端已正确返回 `429`，前端仍只显示“发送失败”，无法让导购理解是会话保护而非系统异常
- 复现步骤：模拟 `sendChat` 返回 `429`
- 根因分类：执行问题
- 修复动作：为 `429` 增加专门的人话提示和重试说明，并补前端回归测试
- 是否补进回归：是
- 当前状态：已修复

### 空状态快捷入口仍映射到固定 demo 标题

- 发现版本：`v1.0.0` 开发中
- 场景：移动端空状态快捷入口
- 现象：后端已下发 `quick_prompts`，前端仍把前两个入口翻译成固定 demo 文案
- 复现步骤：修改 bootstrap 返回的 `quick_prompts`，打开空状态首页
- 根因分类：执行问题
- 修复动作：快捷入口直接渲染 bootstrap 下发内容，并补前端回归测试与 E2E 修正
- 是否补进回归：是
- 当前状态：已修复
